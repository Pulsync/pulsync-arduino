/**
 * pulsync_scheduler — Non-blocking task scheduler (C API)
 *
 * millis()-based timing. Supports setTimeout, setInterval, clearTimeout,
 * clearInterval, every, when. Max tasks configurable via PULSYNC_MAX_TASKS.
 */

#pragma once

#ifdef __cplusplus
extern "C" {
#endif

// TODO: C API declarations

#ifdef __cplusplus
}
#endif

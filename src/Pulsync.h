/**
 * Pulsync — Non-blocking task scheduler + real-time cloud sync for ESP32
 *
 * Main include file. Include only this header in your sketch/project.
 *
 * Usage:
 *   #include <Pulsync.h>
 *
 *   void setup() {
 *     Pulsync.setWiFi("SSID", "password");
 *     Pulsync.begin("PUL-XXXXXX");
 *     Pulsync.setInterval(myFunction, 5000);
 *   }
 *
 *   void loop() {
 *     Pulsync.loop();
 *   }
 *
 * https://github.com/pulsync/esp32
 * License: MIT
 */

#pragma once

// TODO: Implementation
// This file will include the C++ wrapper class (PulsyncClass)
// that wraps the C core modules.

